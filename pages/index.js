import Navbar from '../components/NavBar/Navbar';
import { Layout } from '../layout/Layout';

const Home = () => {
    return (
        <div>
            <Layout></Layout>
        </div>
    );
};

export default Home;
